"use client";

/**
 * Customer Android APK is retired. Guests order on the web / table QR.
 * Kept as a no-op so old imports do not break.
 */
export function InstallAppBanner(_props?: {
  tenantCode?: string;
  restaurantName?: string;
  logoUrl?: string;
}) {
  void _props;
  return null;
}
