"use client";

import { PosPrinterPanel } from "./PosPrinterPanel";

/** Settings still has a Printer card — same AsFix POS task as /printer and POS. */
export function AdminThermalPrinterCard() {
  return <PosPrinterPanel />;
}
